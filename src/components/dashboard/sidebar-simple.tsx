'use client'

// Archivo DUMMY completamente simple - SIN IMPORTS

export function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) {
  return (
    <div>
      <p>Dummy sidebar funcionando</p>
    </div>
  )
}

export default Sidebar
